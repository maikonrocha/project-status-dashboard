{
    "name": "project-dashboard-v3",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "pnpm --parallel -r dev",
        "build": "pnpm -r build",
        "test": "pnpm -r test",
        "lint": "pnpm -r lint"
    },
    "devDependencies": {
        "typescript": "^5.6.3"
    },
    "pnpm": {
        "onlyBuiltDependencies": [
            "@prisma/engines",
            "prisma",
            "@nestjs/core",
            "bcrypt",
            "sharp",
            "unrs-resolver"
        ]
    }
}